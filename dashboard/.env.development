# Dev dashboard uses the Vite proxy (/api → http://127.0.0.1:2785).
# Start both services from the repo root: npm run dev
# Only set VITE_API_URL if you run the API on a different host/port without the proxy.
# VITE_API_URL=http://127.0.0.1:2785
